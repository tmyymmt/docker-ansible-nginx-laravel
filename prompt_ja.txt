----------------------------------
Rocky Linux 9.3で使用する、Ansibleのplaybookファイルを作成。

Ansibleはローカルで実行する。
connection: localを指定する。
適宜 block: を使用して、タスクをグループ化する。

下記をdnfでアンインストールする。
・curl-minimal

下記をインストールして、設定する。
バージョン未指定のものは、適切なタスクでまとめてdnfでインストールする。
ソースコードをダウンロードするものは/usr/local/srcにダウンロードし、/usr/localにインストールすること。
バージョンは上記にまとめて変数定義にすること。
URLは、バージョンの変数を使用し、上部にまとめて変数定義すること。
シェルの実行スクリプトは行を変えて記述すること。
・emacs
・w3m
・gcc
・make
・openssl-devel
・curl-devel
・zlib-devel
・libxml2-devel
・gmp-devel
・sqlite-devel
・pcre-devel
・php 8.2.7
　・ソースからビルド
　　・configureオプションに --enable-fpm --with-openssl --with-curl --with-zlib --with-libxml --with-gmp を付けて
・composer
・nginx 1.22.0
　・ソースからビルド
　　・configureオプションに --with-http_ssl_module --with-http_v2_module を付けて
　・サーバ名はlocalhost
　・httpsについて
　　・SSL/TLSでアクセスできるようにキー生成して設定
　・httpについて
　　・httpアクセスはhttpsにリダイレクト
　・php-fpmを設定して、phpを実行できるように設定

/usr/local/nginx/html/test.php として、下記の内容のファイルを作成する。
<?php phpinfo(); ?>

nginxはsystemdで管理するように設定する。

----------------------------------
Rocky Linux 9.3で使用する、Ansibleのplaybookファイルを作成。

Ansibleはローカルで実行する。
connection: localを指定する。
適宜 block: を使用して、タスクをグループ化する。

php 8.2.7、nginx 1.22.0はインストール済みの前提。

LaravelとNext.jsを使用してプロジェクトを作成する。
